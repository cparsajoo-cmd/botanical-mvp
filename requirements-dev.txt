# Dev/CI-only dependencies. NOT installed on Streamlit Cloud (which only
# reads requirements.txt) — this file is for running the test suite
# locally or in GitHub Actions.
-r requirements.txt
pytest==8.3.4
